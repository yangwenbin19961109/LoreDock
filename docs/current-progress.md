# LoreDock 当前开发进度

- 记录日期：2026-09-20
- 当前分支：`main`
- 当前里程碑：Phase 7 第 9、10 项已完成；第 6 项首个 Windows x64 预览安装包已由 GitHub Actions 构建并附加到预发布页，干净虚拟机安装矩阵与传递依赖声明仍待验收；第 7 项已调整到二期

## 已完成

### 本次阶段存档

- [D-A01：菜单切换后导入资料不再显示](desktop-acceptance-issues.md) 已关闭：只读检查确认本次现场数据完整，列表刷新修复和浅色/深色浏览器回归通过，2026-09-07 用户完成桌面人工验收。
- 收藏操作已改为空心/点亮星标，删除资料改为并排垃圾桶图标；最近使用/收藏列表增加独立主题表面、边框及明确的悬停/选中状态。2026-09-07 用户确认本轮人工验收完成。

- Phase 4 本轮人工验收已由用户确认通过，包括本地自检、手动配置与 P4-A01 修复。Codex 接入与重开恢复此前已确认；Cursor 实机验收继续暂缓，不视为通过。
- MCP 六工具、持久授权、系统凭据、Windows bridge、配置兜底、本地自检及契约基线已实现；本地自检不代表外部 Agent 握手成功。受限非交互自动配置失败记录继续保留。
- 最近使用/收藏 Core 与 UI 已接入：导航可点击、收藏切换、分页刷新、点击预览与显式访问记录。最近记录最多 100 条；自动选中、后台读取和 MCP 查询不记录。删除资料/库级联清理，取消收藏保留资料和访问记录。
- 集合页提供前 8000 字符的安全文本预览，直接跳转主知识库具体分页位置未实现。最近使用/收藏仍待用户人工验收。
- 用户要求重启后，已通过 SQLite backup API 备份 app.sqlite 和 agent-connections.sqlite 至用户数据目录 backups/before-v5-20260904-155224。备份不进入仓库，不包含原始资料副本或系统凭据。
- 新版 Core 与桌面程序已启动，实际元数据库版本确认是 v5。迁移不改变检索索引；旧 Core 不支持直接读取 v5，不应修改 user_version 降级。详见 [ADR 0014](decisions/0014-source-activity.md)。
- 最近验证：2026-09-07 Phase 5 第一批 Ruff、Pyright、Core 全量测试（97 项通过、1 项系统测试默认跳过）、Web ESLint、37 项测试、生产构建及 7 项浏览器测试通过。保留既有 Starlette/httpx 弃用警告；本批未重建 Windows 独立 Core 或安装包。
- [D-A02 与 D-A03](desktop-acceptance-issues.md) 均由用户复验通过并关闭。Phase 5 第二批完成 PPTX/XLSX 依赖、许可证、打包面和定位模型评估，接受 [ADR 0015](decisions/0015-ooxml-readonly-ingestion.md) 的受限只读 OOXML 方案。
- 安全 OOXML ZIP/XML/关系基础层已实现并纳入定向攻击性测试；PPTX 已按演示顺序提取文本框和表格，并以幻灯片页码提供引用定位，Core/API/UI 已开放 `.pptx` 导入且桌面人工验收通过。演讲者备注、图片 OCR、动画和版面还原不在首版范围。
- XLSX 已按工作簿顺序提取共享/内联字符串、数值、布尔值和公式缓存值，保留工作表名称、状态、单元格坐标和工作表页码；Core/API/UI 已开放 `.xlsx`，桌面真实文件人工验收通过。
- URL 第三批采用 [ADR 0016](decisions/0016-bounded-url-snapshots.md) 的受限一次性快照：公网地址与 DNS 固定连接、逐跳重定向复验、HTTPS 降级、响应类型和 10 MiB 上限已实现；数据库 v6 保存来源类型和最终网址。代理 Fake-IP 通过只针对域名的 Cloudflare DoH 公网复核兼容，真实 Wikipedia 抓取成功；保留地址字面量仍拒绝。网页与本地文件入口已共同归入“资料”面板。Core 132 项、Web 39 项和 URL 浏览器回归通过。
- 2026-09-07 用户完成 URL 快照桌面人工验收，第三批关闭；该验收不扩大允许的网络目标，也不代表任意登录态、动态渲染或内网页面受支持。
- 文件夹递归导入第四批已按 [ADR 0017](decisions/0017-user-selected-folder-import.md) 接入：用户选择目录后先在本地预检，限制 500 份受支持资料、单文件 100 MiB 和总体积 1 GiB；确认后逐项复用现有上传接口，显示新增、重复、失败和跳过数量，支持停止后续导入及失败/未完成项重试。未赋予 Core 任意目录读取权限，也未实现长期监听。
- 2026-09-07 用户完成人工验收，文件夹递归导入第四批关闭。项目负责人随后将[文件夹增量监听](folder-monitoring-backlog.md)调整为二期功能，一期不实现持久目录授权、后台变化检测或自动重建。
- PPTX 自动验证已通过：Ruff、Pyright、Core 110 项测试（另 1 项系统测试默认跳过）、Web 格式/lint/类型检查、38 项单元测试及 2 项浏览器契约测试。因测试程序正在运行并占用 Rust 构建目录，本批尚未重建桌面二进制和安装包。
- 批量任务管理第一步已接入：文件和 URL 的公开导入 API 保存原始副本后返回 `pending`，Core 单执行器后台完成索引；运行任务重启后安全回到队列，取消在阶段边界生效，失败或取消任务可异步重试。Web 会继续显示 Core 索引阶段，不再把“上传已接受”直接显示为完成。详见 [ADR 0018](decisions/0018-persistent-source-job-runner.md)。
- 2026-09-08 第五批第一步全量质量门禁通过：Prettier、ESLint、TypeScript、Web 44 项单元测试、11 项 Playwright 浏览器测试、生产构建、Ruff、Pyright、Core 135 项测试（另 1 项系统测试默认跳过）、100 条检索评测、rustfmt 与 Clippy 均通过。保留既有 Starlette/httpx 弃用警告；未重建独立 Core 或安装包。
- 第五批第二步已完成：app.sqlite v7 新增持久化批次与批次条目，任务可选关联批次；重复资料保留批次统计但不重复索引。批次提供封存、暂停、继续、取消和失败项重试，暂停会阻止新租约并让运行项在阶段边界回到等待态。最近批次可在 Core 或界面重启后恢复进度卡；未携带 `batch_id` 的旧客户端继续使用原单任务契约。
- 2026-09-08 第二步全量质量门禁通过：Prettier、ESLint、TypeScript、Web 45 项单元测试、11 项 Playwright 浏览器测试、生产构建、Ruff、Pyright、Core 137 项测试（另 1 项系统测试默认跳过）、100 条检索评测、rustfmt 与 Clippy 均通过。保留既有 Starlette/httpx 弃用警告；未重建独立 Core 或安装包。
- 2026-09-08 用户完成第五批桌面人工验收，确认批次重启恢复、暂停/继续、取消、失败项重试以及 D-A05 的首次批量导入修复均通过。第五批核心范围关闭；逐条批次详情及过期历史清理已调整到二期。
- 专用分块第三轮：固定集扩至 17 份资料、120 条问题，新增跨行表格、跨函数代码和跨 Child 长文档夹具；评测输出新增索引构建耗时、索引体积和查询 P50/P95。真实 E5 同集结果中，两种策略 Recall@10 同为 0.935、片段覆盖 0.942、引用准确率 1.0；通用策略 Top-1/MRR 为 0.725/0.825，候选为 0.708/0.819。该设备上的 E5 通用/候选 P95 为 13.2/30.5 ms，索引体积均约 1.99 MiB。候选未带来稳定质量或延迟收益，生产默认继续为 `generic`；评测 CLI 可显式选择候选复现。
- 1 万真实 E5 分块基准已完成：每批 128 条实际 E5 编码，建库 41.06 秒，100 次查询平均 43.82 ms、P50 39.70 ms、P95 57.72 ms，索引约 19.09 MiB，达到 P95 不高于 1 秒的目标。基准工具新增分批建库与可选进度诊断，避免一次性编码造成不必要的内存峰值。
- 评测覆盖审计已接入报告：逐题明细保留标签，并输出稳定排序的 `tag_case_counts`。固定集已达到首个门槛，共 22 份资料、200 条问题；跨 Child、跨语言和引用边界覆盖分别为 13、46、17 条。200 条真实 E5 对照中，`generic`/`semantic_markdown` 的 Recall@10 均为 0.906、片段覆盖均为 0.900、引用准确率均为 1.0；候选 Top-1/MRR 为 0.660/0.759，通用策略为 0.650/0.753。候选仅带来轻微排序变化，没有实质召回或上下文收益，生产默认继续使用 `generic`。
- 2026-09-09 已基于 Phase 5 最新代码重新构建 Windows 独立 Core、MCP bridge、Tauri Release、MSI 与 NSIS。打包版烟雾测试通过鉴权、持久化后台导入、索引完成、来源就绪和优雅退出；Core 与 bridge 的 Tauri resource 副本 SHA-256 与源构建一致。随后修复 Release 桌面主程序被链接为 Console 子系统导致启动时出现命令行窗口的问题；新构建 PE 子系统已确认为 Windows GUI（值 2），后台 Core 继续使用 `CREATE_NO_WINDOW`，MCP bridge 继续保留标准输入输出。更新后的 MSI 为 68,921,281 字节，SHA-256 `32B6F2DDE3E2D1C44B2DD438D405A1F19622E71ED89A81611A3DC1B5DC0220B7`；NSIS 为 53,595,921 字节，SHA-256 `A7538D104A44E44D5A872BBE955D54B8BC73813C93CFB80911537E52FBBD8BC6`。项目负责人已确认两个安装包的安装、启动、卸载、数据保留及无命令行窗口人工验收通过。
- Phase 7 首批托管备份与恢复已实现：`app.sqlite` 和各知识库索引使用 SQLite Backup API，归档包含原件、解析产物与 manifest，并记录逐文件大小和 SHA-256。Core/API 支持创建、列表、显式完整性校验和恢复排期；桌面设置页提供对应入口。恢复前自动保存当前状态，只在 Core 重启且数据库未打开时切换，失败回滚原数据。全量门禁通过 144 项 Core 测试（另 1 项系统测试默认跳过）、46 项 Web 测试、12 项浏览器测试、200 条检索评测以及全部格式、lint、类型、构建和 Rust 检查；2026-09-09 项目负责人已确认桌面真实数据人工验收通过。验收包 MSI 为 68,941,761 字节，SHA-256 `59DAC7842F373C94893257E81A5B4547F772B78B221A164B38B2658127D3F35B`；NSIS 为 53,623,784 字节，SHA-256 `794454CB9EFD85F5BAAD624929D9CDAE1B3FD9CC7D3E78582DC0EF5678755897`。外部归档导入、自动清理、加密、Agent 连接元数据与模型缓存不在首批范围，见 [ADR 0019](decisions/0019-managed-backup-restore.md)。
- Phase 7 第 2 项版本迁移闭环已实现：Core 集中维护应用数据库与索引构建契约版本，`/version` 握手返回 Core、API、数据库和索引版本；桌面端拒绝任一版本不匹配的运行时组合。应用数据库继续按顺序确定性前向迁移，更高版本会在设置 WAL 或修改 schema 前被拒绝；旧索引继续从原始资料在旁路文件中重建并原子切换。定向 Core 与 Rust 测试通过，见 [ADR 0020](decisions/0020-runtime-version-compatibility.md)。
- Windows 独立 Core、MCP bridge、Tauri Release、MSI 与 NSIS 已按版本迁移代码重新构建；打包态 Core 鉴权、导入、索引和优雅退出烟雾测试通过。MSI 为 68,945,857 字节，SHA-256 `EE2378116F5EB69F3786EFDAE38184D42D976785C2C8646D996264FA2B87BD4E`；NSIS 为 53,629,538 字节，SHA-256 `8350314B7E0BB821E748DC079204B382A5A0F3A23BFDE50E0867C3844C38E08B`。
- Phase 7 第 3 项模型安装能力复核完成：固定 revision、资产大小与 SHA-256 校验、HTTP Range 断点续传、临时文件原子替换、磁盘空间预检、持久下载任务及进程中断恢复均已存在，相关聚焦测试通过。
- Phase 7 第 4 项安全覆盖复核完成：路径越界、OOXML 展开数量/体积/压缩比、SSRF 与重定向复验、HTML/XSS 惰性文本渲染、MCP 授权与响应边界均有自动回归；相关 Core 安全测试与 46 项 Web 测试通过。
- Phase 7 第 5 项恢复测试已补齐：30 份资料连续导入与 100 次检索保持一致；源文件写入中断不保留 `.part`；元数据库写满会删除已落盘但未登记的原件并返回稳定错误；损坏索引和中断遗留的 `.rebuild` 会从原始资料重建。结合既有运行任务重启恢复与损坏备份拒绝测试，16 项聚焦测试通过。
- Phase 7 第 8 项发布文档已补齐：README 现在提供用户指南、Windows 部署、故障排查与 Agent 接入入口；文档只描述已实现的一期 Windows 本地能力，并明确服务器、跨平台和 Cursor 实机状态。
- Phase 7 第 9 项许可证清单已完成直接依赖、运行时下载模型和字体的来源核对；2026-09-20 项目负责人确认图标、UI mockup 与测试图片均为项目专门生成的自有素材。第 10 项保留 PolyForm Noncommercial，并在 README 提供非商业许可说明、第三方清单和商业授权联系入口。安装包内的第三方声明仍需在第 6 项最终打包时按实际内容处理；第 7 项已调整到二期。
- Phase 7 第 6 项本机打包更新：Windows Core、MCP bridge、Web、Tauri Release、MSI 和 NSIS 已按当前代码重建；打包 Core 的启动、401 鉴权、导入索引及退出烟雾测试通过。安装资源新增项目 PolyForm 许可证与直接依赖清单，MSI/NSIS 生成脚本均确认包含两份文件。传递依赖完整声明、干净虚拟机安装/覆盖升级/卸载矩阵和人工界面验收仍未完成，不将本机产物视为正式发布版。
- 2026-09-20 的 `v0.1.0-preview.1` 已通过 GitHub CI；Windows runner 从该 tag 构建 MSI/NSIS 并通过打包 Core 烟雾测试，两个安装包已附加到[预发布页](https://github.com/yangwenbin19961109/LoreDock/releases/tag/v0.1.0-preview.1)。此验证不包含实际安装、升级或卸载；后续 tag 的流水线已接入相同打包步骤。发布前剩余事项与可复现操作见 [Windows 预览版发布核查](windows-preview-release-checklist.md)。
- D-A05 已修复：上传请求不再检查或重建索引；后台执行器在索引锁内完成契约检查，索引提交与 manifest 更新共享同一锁边界，搜索检查与读取也已串行。现场已接收任务均成功，未删除或重建用户资料。新增隔离回归后全量结果为 Core 138 项通过（另 1 项系统测试默认跳过）、Web 45 项和 Playwright 11 项通过；下一步用 10 份以上资料桌面复验首次批量导入。
- 测试程序保持运行供验收。本次提交和推送不包含安装包、模型、数据库、备份、私人资料、凭据或运行日志。

### Core 与检索

- 知识库和资料的创建、读取、重命名、删除及完整数据清理。
- Markdown、TXT、PDF、DOCX、PPTX、XLSX、HTML/HTM 导入、解析、分块、持久化任务与失败重试。
- Phase 5 第一批 HTML/HTM 使用标准库安全提取文本，不执行脚本或加载外部资源；代码、自动回归和桌面真实文件验收均已通过，详见 [Phase 5 交付记录](phase-5-delivery.md)。
- SQLite FTS5、sqlite-vec、BM25-only 降级、向量召回和 RRF 混合检索。
- Child 排名、Parent/相邻 Child 按需上下文扩展和精确引用范围。
- multilingual-e5-small INT8 ONNX 本地 Provider、模型资产安装与校验。
- 22 份资料、200 条问题的固定检索评测集和多策略对照。
- 资料列表服务端 keyset 游标分页、筛选与排序。

### Web 与桌面

- 按既有 UI 渲染图实现左侧导航、中间资料工作区、右侧预览详情的三栏界面。
- 创建、切换、重命名和删除知识库；选择、拖放、导入和删除资料。
- 混合搜索、BM25-only 开关、结果上下文展开、引用定位和原文高亮。
- 任务进度轮询、失败提示与重试。
- 资料列表服务端筛选、排序及每页 10 条游标分页。
- Tauri 自动启动 Core、动态回环端口、一次性 Bearer Token、API 版本握手和优雅关闭。
- Windows PyInstaller 独立 Core 构建及 Tauri resource 装配。
- Windows MSI 与 NSIS 本地验收包构建。
- Core 异常退出后的三次有界自动恢复、状态诊断、诊断复制和人工重启。
- Windows MSI/NSIS 的一次性虚拟机验收脚本和人工验收矩阵。
- Core 持久化的首次启动状态、明暗主题和默认搜索方式，以及对应桌面设置界面。
- 默认本地模型状态、磁盘预检、固定版本下载、SHA-256 校验和启用提示。
- 持久化模型下载任务、真实字节进度、暂停、重试、进程中断恢复和 HTTP Range 断点续传。
- 保留引用偏移的结构化文档预览，以及不执行资料 HTML/脚本的安全渲染。
- `system` 主题跟随系统偏好，明暗主题三栏界面已完成真实资料视觉走查。
- 整个工作区文件拖放、搜索结果预览定位、搜索重置、融合相关度展示，以及默认/最大化窗口自适应布局。
- BM25、E5 vector-only 与等权 RRF 混合检索的 100 条同集排序诊断；评测报告可记录逐题查询和分数。

## 已验证

- Python：Ruff 格式与 lint、Pyright 和 73 项默认 pytest 测试通过；系统凭据测试默认跳过，另行显式执行打包版测试通过（2026-09-04）。
- Web：Prettier、ESLint、TypeScript、31 项 Vitest 测试、5 项浏览器测试和生产构建通过；本轮新增自检 UI 回归，不替代桌面实测。
- 最新契约检查：新增测试文件 Ruff 检查与格式检查、全量 Pyright、83 项默认测试通过，1 项系统实测默认跳过；测试报告含一条 Starlette/httpx 弃用警告。未重新打包或调用真实模型；本轮未修改 UI，Web 检查结果沿用上一轮。
- Rust：`cargo check`、`cargo test` 和 Tauri Release 构建通过。
- 独立 Core：动态端口、401 鉴权、健康检查、创建知识库、Markdown 导入/索引和优雅退出通过。
- 发行目录桌面程序：确认从 Tauri resource 启动独立 `loredock-core.exe`，关闭后无残留进程。
- 故障恢复：强制终止 ready Core 后以新 PID 和动态端口恢复；持续启动失败在三次恢复后停止，无无限重启或孤儿进程。
- Windows 构建产物：MSI 和 NSIS 均成功生成；二进制产物位于 Git 忽略目录，不提交到仓库。

## 当前限制

- Phase 2.5 固定评测已达到 200 条首个门槛，真实 E5 多策略复验和 1 万分块性能验证均已完成；候选分块不切换生产默认。后续仅按明确质量缺口继续扩展到 500 条。
- Windows MSI 与 NSIS 的安装、启动、卸载、数据保留及无命令行窗口人工验收已由项目负责人确认通过；代码签名仍未完成，覆盖升级自动矩阵仍保留为发布前补充验证。
- macOS、Linux 的独立 Core、桌面打包和生命周期尚未验证。
- 多模型切换与 PDF/DOCX 原始版面还原尚未实现；当前提供安全的结构化文本预览。
- Phase 4 已实现六工具、持久授权 API、系统凭据适配器、稳定 ID stdio bridge 与“复制给 Agent”界面；Windows 独立打包与系统凭据重连实测已通过，真实 Agent 和安装矩阵验收尚未完成，见 [Phase 4 交付记录](phase-4-delivery.md)。
- 搜索排序已有可解释分数与首轮同集诊断；融合权重、分块、模型和 reranker 调优按项目决定推迟到真实 Agent/MCP 接入之后。
- 已记录第二期 [Agent Skill 管理功能](agent-skill-management.md)：计划通过 UI 管理本地 Skill 的导入、预览、启停、Agent 分配、配置生成、更新和移除；不属于当前一期 Phase 0～7，本期不开发，也不阻塞一期验收和发布。
- 已记录第二期 [用户与 Agent 对话归档](agent-conversation-archive.md)：用户可主动导入、浏览、筛选和备份历史对话，并明确选择是否加入知识库；默认不扫描第三方客户端数据、不进入知识库检索或 MCP，本期不开发，也不阻塞一期验收和发布。
- 已记录第二期 [文件夹增量监听](folder-monitoring-backlog.md)：二期再实现持久目录授权、重启恢复、变化防抖、移动/重命名识别和自动增量重建；一期只保留已验收的一次性文件夹递归导入。
- 资料及可读 Markdown/JSON 导出已调整到第二期，不属于当前一期 Phase 0～7，也不再作为 Phase 5 或一期发布的验收条件；二期实现仍需保持格式可读、可迁移并避免产品锁定。
- 搜索关联图片展示及批量任务逐条详情、过期历史清理已调整到第二期，不属于当前一期 Phase 0～7，也不再作为 Phase 5 或一期发布验收条件。
- 日志导出、进一步的隐私脱敏策略和问题诊断包已调整到第二期；一期保留现有脱敏状态诊断与手动复制能力，该增强不再阻塞一期发布。
- 已记录 [Agent 客户端兼容性扩展](agent-client-compatibility.md)：手动添加将从 Codex/Cursor 固定选项演进为可扩展客户端适配器和通用 MCP 兜底；当前仅记录，尚未实现，也不把未经真实验收的客户端宣称为已支持。
- 已记录 [搜索关联图片展示优化](image-preview-backlog.md)：项目不引入本地 OCR；后续保留图片与来源页码/结构锚点，在搜索命中文字时展示相关原图。该增强不阻塞当前 XLSX 验收。

## Phase 3 人工验收问题

第一次人工验收记录了 6 项待整改问题：拖放导入不可用、搜索结果未驱动预览定位、预览标题与删除按钮重叠、排序缺少相关度与诊断信息、缺少搜索重置，以及默认/最大化窗口的响应式布局和滚动归属不符合预期。详细验收条件与分批安排见 [Phase 3 阶段验收问题与修复排期](phase-3-acceptance-issues.md)。

2026-09-04 收尾复验：`pnpm check` 全部通过（格式、lint、类型检查、单元测试、Web 生产构建和 Tauri 无安装包 Release 构建）；Web 的 12 项测试、1 项契约测试、1 项浏览器离线烟雾测试，以及 Rust 格式检查和 3 项测试通过。浏览器烟雾测试仅覆盖 Core 不可用提示，不代表预览定位与窗口布局人工验收通过。桌面开发程序已启动供人工复验；尚未提供可执行安装矩阵的干净 Windows 虚拟机，未在开发机执行安装/卸载测试。

## 下一步执行顺序

Phase 4 人工验收 P4-A01 已完成代码修复与浏览器回归，待用户桌面复验；未改变文本框宽度或复制行为，详见 [Agent 接入验收问题](agent-acceptance.md#phase-4-人工验收问题)。

2026-09-04 用户确认：搜索结果预览定位与高亮、搜索重置、长文件名与删除按钮隔离、默认/最大化窗口布局均验收通过。此前拖放导入也已确认通过；未单独记录的系统缩放组合不据此声称已实测。

1. 已完成：Phase 3 最后一轮功能与布局人工复验（P3-A02、P3-A03、P3-A05、P3-A06）。
2. 2026-09-04 项目负责人同意安装矩阵后置为发布前必验项；Phase 3 不声称安装验收完成，但不再阻塞 Phase 4。
3. Phase 4 第五批界面与说明、Windows 独立 bridge 已实现；打包版系统凭据库、stdio 与 Core 换端口重连已通过。下一步进行真实 Agent 配置、搜索与引用读取验收，不复制明文令牌。
4. Phase 4 本轮桌面人工验收已通过，Cursor 实机验收暂缓；保留受限非交互自动配置失败记录。下一步验收最近使用/收藏的真实重启保留及删除清理，之后再确认 Phase 5 排期，详见 [Agent 接入验收](agent-acceptance.md)。
5. Agent/MCP 真实接入并积累代表性查询后，重新开启 P3-A04 排序质量优化，扩充固定评测并评估加权融合、分块、模型与轻量 reranker；基线见 [检索排序诊断](retrieval-ranking-diagnosis.md)。
6. 非阻塞质量工作按新发现的明确缺口继续扩展评测；200 条固定集和 1 万真实 E5 分块性能报告已经完成。

详细设计和验收条件以 [技术架构](technical-architecture.md)、[开发计划](development-plan.md)、[Phase 2.5 交付记录](phase-2.5-delivery.md)、[Phase 3 交付记录](phase-3-delivery.md)和 [Phase 3 阶段验收问题与修复排期](phase-3-acceptance-issues.md)为准。
